{
  "name": "@tuma/backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "worker:settlement": "bun run src/workers/settlement.worker.ts",
    "worker:escrow": "bun run src/workers/escrow.worker.ts",
    "worker:rail": "bun run src/workers/rail.worker.ts",
    "worker:notify": "bun run src/workers/notify.worker.ts",
    "db:push": "drizzle-kit push --force",
    "db:migrate": "drizzle-kit migrate",
    "db:generate": "drizzle-kit generate",
    "db:studio": "drizzle-kit studio",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tuma/shared": "workspace:*",
    "hono": "^4.6.0",
    "@hono/node-server": "^1.13.0",
    "drizzle-orm": "^0.36.0",
    "postgres": "^3.4.5",
    "ioredis": "^5.4.1",
    "bullmq": "^5.13.0",
    "viem": "^2.21.0",
    "jose": "^5.9.6",
    "zod": "^3.24.2",
    "@hono/zod-validator": "^0.4.1"
  },
  "devDependencies": {
    "drizzle-kit": "^0.28.0",
    "typescript": "^5.8.3",
    "@types/bun": "latest"
  }
}
